<Window x:Class="Boletera.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Boletera.Views"
        xmlns:CustomControls ="clr-namespace:Boletera.CustomControls"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800"
        
        xmlns:ViewModel="clr-namespace:Boletera.ViewModel">



    <Window.DataContext>
        <ViewModel:LoginViewModel/>
    </Window.DataContext>



    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="CineApp" FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Correo electrónico:"/>
            <TextBox x:Name="txtCorreo"
                       Text="{Binding Username}"
                       Margin="0,5,0,10" Height="30"/>


            <TextBlock Text="contraseña"/>
            <CustomControls:BindablePasswordBox
                        Password="{Binding Password, Mode=TwoWay,
                        UpdateSourceTrigger=PropertyChanged}">

            </CustomControls:BindablePasswordBox>
            <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="#D7596D"
                               FontWeight="Medium"
                               Margin="0,10,0,0"
                               TextWrapping="Wrap"/>

            <Button Content="Iniciar Sesión" 
                    Command="{Binding LoginCommand}"
                    Height="35" 
                    Background="#0078D7" 
                    Foreground="White"
                    FontWeight="Bold"/>
            
        </StackPanel>
    </Grid>
</Window>
